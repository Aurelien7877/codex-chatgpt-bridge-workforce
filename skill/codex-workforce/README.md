# Install in Codex

Copy this folder to `%USERPROFILE%\.codex\skills\codex-workforce` or install it through your normal Codex skill workflow. Restart Codex after installation. The skill is implicit by default; it guides Codex to call the configured `workforce_delegate` MCP tool when useful.
