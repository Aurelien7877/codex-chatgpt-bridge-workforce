{
  "name": "codex-workforce",
  "version": "0.3.0",
  "description": "Token-efficient multi-model delegation for Codex MCP with OpenCode and ChatGPT",
  "type": "module",
  "bin": {
    "codex-workforce": "dist/cli.js",
    "codex-workforce-mcp": "dist/mcp.js"
  },
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test test/*.test.mjs",
    "lint": "eslint ."
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.70.1"
  }
}
